{
  "name": "tms-enterprise-app",
  "version": "1.0.0",
  "private": true,
  "description": "TMS Enterprise App – Nx monorepo (tms-web, tms-api, shipment-service)",
  "overrides": {
    "@types/express": "4.17.21",
    "@types/express-serve-static-core": "4.19.5"
  },
  "scripts": {
    "build": "nx run-many -t build",
    "serve": "nx run-many -t serve --parallel=1",
    "test": "nx run-many -t test",
    "lint": "nx run-many -t lint",
    "tms-api:build": "nx run tms-api:build",
    "tms-api:serve": "nx run tms-api:serve",
    "tms-web:build": "nx run tms-web:build",
    "tms-web:serve": "nx run tms-web:serve",
    "shipment-service:build": "nx run shipment-service:build",
    "shipment-service:serve": "nx run shipment-service:serve"
  },
  "workspaces": [
    "apps/tms-api",
    "apps/tms-web"
  ],
  "devDependencies": {
    "nx": "^20.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "rxjs": "^7.8.2"
  }
}
