# tms-api – copy to .env.development (or .env.production / .env.stage)

# Server
APP_HOST=0.0.0.0
APP_PORT=3010

# Optional: PostgreSQL (app runs without it; uses in-memory data when not connected)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USERNAME=
DB_PASSWORD=
DB_DATABASE=nodetest

# Optional: delegate shipment CRUD to shipment-service (must be running on 8081)
SPRING_BOOT_SHIPMENT_URL=http://localhost:8081

# Optional: JWT, search, mail, AWS – set if you use those features
# JWT_SECRET_KEY=
# MANTRA_SEARCH_URL=http://localhost:7700
# MANTRA_SEARCH_API_KEY=
# SMTP_HOST= SMTP_USER= SMTP_PASSWORD=
